# Doubleword API Authentication
# Copy this file to .env and fill in your actual token
# IMPORTANT: Keep .env file private - never commit to version control

# Your Doubleword API authentication token (REQUIRED)
DOUBLEWORD_AUTH_TOKEN=sk-your-token-here

# All other settings (model, endpoints, timeouts, etc.) are in dw_batch/config.toml
# Only secrets belong in this .env file
